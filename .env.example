DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=1

# 비우면 SQLite 를 씁니다. 운영은 PostgreSQL 을 전제로 합니다.
# DATABASE_URL=postgresql://bordo:bordo@localhost:5432/bordo

# Discord Bot 이 /internal/v1 을 부를 때 쓰는 토큰
BORDO_SERVICE_TOKEN=dev-service-token
